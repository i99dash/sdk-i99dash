{
  "name": "i99dash-example-cluster-hello-world",
  "version": "0.2.1",
  "private": true,
  "description": "Phase A reference mini-app for the i99dash native-capability surface — enumerates displays, picks the cluster, opens a Presentation/overlay surface there with custom HTML.",
  "type": "module",
  "scripts": {
    "dev": "i99dash dev",
    "validate": "i99dash validate",
    "build": "i99dash build",
    "publish": "i99dash publish"
  },
  "devDependencies": {
    "i99dash": "^3.0.0"
  }
}
