{
  "name": "i99dash-example-pkg-launcher",
  "version": "0.10.1",
  "private": true,
  "description": "Phase C reference mini-app — launches installed packages on any display + manages the boot-launch list.",
  "type": "module",
  "scripts": {
    "dev": "i99dash dev",
    "validate": "i99dash validate",
    "build": "i99dash build",
    "publish": "i99dash publish"
  },
  "devDependencies": {
    "i99dash": "^3.0.0"
  }
}
